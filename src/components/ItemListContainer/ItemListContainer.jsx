import React from "react";

function ItemListContainer( { message } ) {

  // const username = "name";

  return (
    <div>
      <h1>{message}</h1>
    </div>
  );
}

export default ItemListContainer;

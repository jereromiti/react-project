import React from "react";

function ItemListContainer( { greeting } ) {

  // const username = "name";

  return (
    <div>
      <h1>{greeting}</h1>
    </div>
  );
}

export default ItemListContainer;
